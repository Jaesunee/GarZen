import {
    StyleSheet,
    Button,
    View,
    SafeAreaView,
    Text,
    Alert,
  } from 'react-native';


const HomeScreen = ({navigation}) => {
    return (
        <Text> Plant some shit </Text>
    );
};

export default HomeScreen